"""Repository layer for database operations.

Provides an abstract base repository and concrete implementations
for each entity. Repositories encapsulate all SQLAlchemy queries
and return Pydantic schemas to the service layer.
"""
