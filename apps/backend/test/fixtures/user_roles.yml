# The `user_roles` table was dropped by migration 20260423000007 (single-role
# model: `users.role` string column). This fixture file is intentionally
# empty so `fixtures :all` doesn't try to insert into a nonexistent table.
